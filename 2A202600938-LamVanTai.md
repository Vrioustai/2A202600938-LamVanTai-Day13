# Báo cáo dự án Observathon — 2A202600938-LamVanTai

**Tóm tắt:**
- **Mục tiêu:** Chẩn đoán và sửa agent thương mại điện tử hộp đen (Observathon). Viết lại `solution/prompt.txt`, cập nhật `solution/config.json`, và thêm lớp `solution/wrapper.py` để quan sát, khử nhiễu, xác thực số học, và giảm thiểu lỗi.
- **Trạng thái hiện tại:** Đã áp dụng cấu hình và wrapper giống repo tham khảo; đã chạy mô phỏng bằng OpenAI và chấm local scorer.

**Kết quả chạy (ngày 2026-06-15):**
- Public score (trước sửa/đã có trong repo): xem [score.json](score.json)
  - Headline: 82.88
- Private run (đã tạo `run_output_private.json` và chấm bằng scorer tại `C:\Users\Vrious\Desktop\observathon-score`): xem `score_private.json`
  - Headline (private): 55.09 (80 câu)
  - Subscores: correct=0.0, quality≈0.183, latency=1.0, cost=1.0, drift≈0.836, prompt≈0.488, diag_f1=1.0
- Ghi chú: bạn cung cấp ảnh chụp kết quả test trước đây (tham khảo user attachment). Không có việc nhận diện cá nhân từ ảnh.

**Các file đã chỉnh sửa / tạo:**
- `solution/config.json` - thay đổi tham số chính (model, temperature, max_steps, context_size, tool_budget,...). (Xem [solution/config.json](solution/config.json) )
- `solution/prompt.txt` - viết lại bằng tiếng Việt, quy định rõ gọi tool, luật từ chối, và định dạng output. (Xem [solution/prompt.txt](solution/prompt.txt) )
- `solution/wrapper.py` - thay thế bằng phiên bản có: phát hiện injection, guardrail xác thực số học, log chi tiết, retry thông minh, cache, redaction. (Xem [solution/wrapper.py](solution/wrapper.py) )
- `solution/findings.json` - báo cáo fault classes (11 mục). (Xem [solution/findings.json](solution/findings.json) )
- Tệp tạo khi chạy: `run_output_private.json`, `score_private.json` (tạo ở thư mục gốc dự án).

**Cách chạy tái tạo (trên Windows PowerShell):**
```powershell
# (thiết lập OPENAI key nếu chưa set)
$Env:OPENAI_API_KEY = 'sk-...'

# Chạy simulator (sử dụng binary public như đã dùng để tạo run_output_private.json)
.\bin\public\observathon-sim-win\observathon-sim.exe --config solution/config.json --wrapper solution/wrapper.py --out run_output_private.json --concurrency 8

# Chấm bằng scorer có trên Desktop
C:\Users\Vrious\Desktop\observathon-score\observathon-score.exe --run run_output_private.json --findings solution/findings.json --team "2A202600938-LamVanTai" --out score_private.json
```

**Phân tích ngắn (vấn đề & gợi ý sửa):**
- Correctness (0/80 trong lần chạy private hiện tại) là vấn đề lớn nhất — agent trả lời sai hoặc không tuân thủ định dạng "Tong cong: X VND".
  - Nguyên nhân khả thi: prompt chưa đủ chặt ở một số edge-case, wrapper chưa khớp hoàn toàn với trace của sim, hoặc model sinh câu trả lời không dùng giá trị tool trả về.
  - Hành động gợi ý: kiểm tra một số mục lỗi cụ thể trong `run_output_private.json` (tìm các trace có thiếu `check_stock` / `get_discount` / `calc_shipping`, hoặc có total không đúng). Sau đó: bổ sung guardrail chặt hơn trong `wrapper.py` (bắt buộc recompute + override answer khi khác), giảm temperature, tăng `self_consistency` hoặc bật `verify` trong `config.json`.

- Prompt-injection: đã thêm sanitize trong wrapper, nhưng cần mở rộng các pattern và test thêm với ghi chú có từ khóa tấn công.

- Drift & Cost: đã tối ưu context_size và cache; giữ `context_reset_every=8` và `max_completion_tokens=120` để cân bằng chi phí/độ chính xác.

**Hành động tiếp theo đề xuất (ưu tiên):**
- A. Phân tích chi tiết 10-20 câu sai đầu tiên từ `run_output_private.json` để tìm mẫu lỗi cụ thể (tôi có thể làm việc này nếu bạn cho phép). File: [run_output_private.json](run_output_private.json)
- B. Tinh chỉnh `solution/prompt.txt` để ép định dạng và giảm lặp/giả mạo giá trị.
- C. Bổ sung/siết guardrail trong `solution/wrapper.py`:
  - Luôn recompute total từ trace và ép trả về đáp án sửa nếu cần.
  - Nếu thiếu tool cần thiết → trả về refusal message theo `findings.json`.
- D. Sau chỉnh sửa, chạy lại private sim và chấm lại; lặp đến khi đạt mục tiêu.

**Tài liệu & tham khảo:**
- Hướng dẫn gửi bài: [docs/SUBMIT.md](docs/SUBMIT.md)
- Thông tin fault classes: [docs/FAULT_CLASSES.md](docs/FAULT_CLASSES.md)

**Ghi chú cuối:**
- Tôi đã tạo file báo cáo này tại: `baocao.md` (ở gốc dự án). Nếu muốn, tôi sẽ tiếp tục phân tích chi tiết các câu sai và tự động đề xuất sửa `prompt.txt` và `wrapper.py`.


---
*Bạn muốn tôi tiếp tục với bước nào tiếp theo?*  
- 1) Phân tích chi tiết `run_output_private.json` (tự động sửa và thử lại), hay 2) Chuẩn bị commit & push các file hiện tại lên Git, hay 3) Gợi ý sửa prompt cụ thể ngay bây giờ?